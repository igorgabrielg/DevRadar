## Rodar na servidor back-end

Instalação das dependencias

### `yarn install -d` or `npm install -d`

Iniciar projeto

### `yarn dev`

Servidor será inicializado com o nodemon

Obs: Toda alteração fará com que o servidor inicie