import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Add na tag que tiver ID root os elementos de App.js
ReactDOM.render(<App />, document.getElementById('root'));
